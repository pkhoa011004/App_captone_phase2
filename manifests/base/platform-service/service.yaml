apiVersion: v1
kind: Service
metadata:
  name: platform-service
  labels:
    app: platform-service
spec:
  selector:
    app: platform-service
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
